# Changes Summary - October 28, 2025

## Username Change: admin → supervisor

### Overview
Changed the admin account username from `admin` to `supervisor` with matching password.

### Changes Made

#### 1. Auth Service Code
**File**: `auth-service/auth_service.py`

Changed USERS dictionary:
```python
# OLD
USERS = {
    "userA": {"password": "passwordA", "groups": ["annotatorA"]},
    "userB": {"password": "passwordB", "groups": ["annotatorB"]},
    "admin": {"password": "admin123", "groups": ["pacsadmin"]}
}

# NEW
USERS = {
    "userA": {"password": "passwordA", "groups": ["annotatorA"]},
    "userB": {"password": "passwordB", "groups": ["annotatorB"]},
    "supervisor": {"password": "supervisor", "groups": ["pacsadmin"]}
}
```

Changed login page test account hints:
```html
<!-- OLD -->
Test accounts:<br>
userA / passwordA<br>
userB / passwordB<br>
admin / admin123

<!-- NEW -->
Test accounts:<br>
userA / passwordA<br>
userB / passwordB<br>
supervisor / supervisor
```

#### 2. Documentation Updates

Updated the following files to reflect the new username:

- **USER-CREDENTIALS.md** - Updated credentials table, testing commands, examples
- **CHANGELOG.md** - Updated user accounts table and user experience references
- **claude.md** - Updated all code examples and test commands

### Current User Accounts

| Username   | Password   | Groups       | Access                     |
|------------|------------|--------------|----------------------------|
| userA      | passwordA  | annotatorA   | Own studies + annotations  |
| userB      | passwordB  | annotatorB   | Own studies + annotations  |
| supervisor | supervisor | pacsadmin    | All studies + annotations  |

### Testing

#### Login Test
```bash
# Test new supervisor credentials
curl -c /tmp/cookies_supervisor.txt -b /tmp/cookies_supervisor.txt \
  -d "username=supervisor&password=supervisor" http://localhost/login

# Verify access to all studies
curl -b /tmp/cookies_supervisor.txt http://localhost/api/dicom-web/studies
# Should return all studies
```

#### Verify Old Credentials Don't Work
```bash
# This should fail
curl -d "username=admin&password=admin123" http://localhost/login
# Should show "Invalid username or password"
```

### Deployment

```bash
# Rebuild auth service
docker build -t ohif/auth-service:latest -f auth-service/Dockerfile auth-service

# Restart auth service
docker stop ohif_auth_service_kc && docker rm ohif_auth_service_kc
docker compose up -d auth_service

# Verify service is running
docker logs ohif_auth_service_kc --tail 10
```

### Impact

✅ **No breaking changes** - Only affects authentication
- Username changed: `admin` → `supervisor`
- Password changed: `admin123` → `supervisor`
- Group remains: `pacsadmin` (unchanged)
- Authorization logic unchanged (still uses `pacsadmin` group)

### Files Modified

1. `auth-service/auth_service.py` - User credentials and login page
2. `USER-CREDENTIALS.md` - Complete rewrite with new credentials
3. `CHANGELOG.md` - Updated user tables
4. `claude.md` - Updated all examples and test commands

### Rationale

The change was made to:
- Use a more descriptive username (`supervisor` vs generic `admin`)
- Maintain consistency between username and password
- Improve clarity in multi-user documentation

---

**Completed**: October 28, 2025
**Applied**: Auth service rebuilt and restarted
**Status**: ✅ Active and tested
